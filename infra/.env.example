# Compose-level environment variables
# Copy to infra/.env and adjust values for your environment

# Google OAuth (ID is not a secret, but avoid hardcoding in source)
GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Optional: override frontend origin if needed
# FRONTEND_ORIGIN=http://localhost:3001
